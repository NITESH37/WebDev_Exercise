// Divide a number using biwise operator and use all bitwise operator

#include <stdio.h>

void main()
{
    // int x = 16, y;
    // y = x >> 3;
    // printf("%d", y);
    // int x = 16, y;
    // y = x << 3;
    // printf("%d", y);
    int x = 5, y = 9, z;
    z = x | y;
    printf("%d", z);
    // int x = 5, y = 9, z;
    // z = x ^ y;
    // printf("%d", z);
    // int x = 5, y = 9, z;
    // ~x;

    // int x;
    // int y;
    // y = sizeof(x);
    // printf("%d", y);
}